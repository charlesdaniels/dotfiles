if [ "$PROVISION_HAVE_ROOT" = "NO" ] ; then
	# we only want to do a non-privileged install

	printf "INFO: installing herc-tools-public.."

	HERC_TOOLS_PUBLIC="$THIRDPARTY_DIR/herc-tools-public"

	run_step "$LOG_DIR/herc_tools_public.log" make -C "$HERC_TOOLS_PUBLIC" install

	echo " DONE"
fi
