#!/bin/sh

# automatically run autobacklight once every 3 minutes, but only if 
# undocked. This gets run by i3 on startup.

set -u
set -e

while true ; do
	DOCK_STATE="$(cat ~/.dspool/dock_status)"
	if [ "$DOCK_STATE" = "UNDOCKED" ] ; then
		~/bin/autobacklight
	fi
	sleep 3m
done
