Overview
========

Perform a full system upgrade via ``pacaur``. This always runs after
``archlinux-update``, so it should only ever handle updating AUR packages.

.. todo::

    ``pacaur`` is no longer updated. A newer tool should be selected to replace
    it.
