Overview
========

This task installs my personal LaTeX stylesheets into ``~``. You can use them
via ``/usepackage{cad-next}``, which is the most current stylesheet.  The
stylesheet ``cad-standard`` is maintained for backwards-compatibility with
existing documents.
