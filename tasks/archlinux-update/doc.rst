Overview
========

This task performs a full, unattended system upgrade of an Archlinux, namely by
running ``pacman -Syu --noconfim``. You should probably keep an eye on the
Archlinux mailing list to ensure this is safe before running this task.
