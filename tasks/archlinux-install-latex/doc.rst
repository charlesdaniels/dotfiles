Overview
========

Install LaTeX on Archlinux. This is a separate task so it can be omitted from
later tasks if desired. This is useful since LaTeX is a large download, and is
not always required, especially for server-oriented systems.
