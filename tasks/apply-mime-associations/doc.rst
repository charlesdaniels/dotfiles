Overview
========

This task applies my preferred MIME associations using XDG. The associations
are specified in ``mimeassoc.tsv``, which has the schema ``MIME type, desktop
file``. ``lsdesktopf`` is used to ensure that the given desktop file exists
before changing the association.
