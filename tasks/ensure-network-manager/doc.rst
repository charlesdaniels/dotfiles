Overview
========

Ensure that NetworkManager is installed and enabled, installing and enabling it
if it is not already. This is a generic task which uses a ``require-any`` to
make sure NetworkManager is enabled appropriately for whatever distro is in
use.
