#!/bin/bash
# installs utilities I like
package-manager-install(vim   ) 
package-manager-install(tmux  )
package-manager-install(htop  )
package-manager-install(uptime)
package-manager-install(iotop )
package-manager-install(nmap  )
package-manager-install(wget  )
package-manager-install(curl  )
package-manager-install(git   )