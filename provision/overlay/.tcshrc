# Setup for my custom paths
setenv EDITOR vim
setenv VISUAL vim
setenv GOPATH $HOME/.go-workspace

setenv PATH $HOME/.cargo/bin:$PATH
setenv PATH $HOME/bin:$PATH
setenv PATH $HOME/.go-workspace/bin:$PATH
setenv PATH $HOME/.net.cdaniels.toolchest/bin:$PATH
setenv PATH $HOME/.net.cdaniels.toolchest/local/bin:$PATH
setenv PATH /opt/bin:$PATH
setenv PATH /opt/local/bin:$PATH
setenv PATH /opt/net.cdaniels/bin:$PATH

#export PS1="[\u@\h][\A][\w]\n(bash) $ \[$(tput sgr0)\]"
set prompt='[%n@%m][%T][%~]\n(tcsh) \$ '
setenv LC_ALL en_US.UTF-8
setenv LANG en_US.UTF-8
setenv LANGUAGE en_US.UTF-8

