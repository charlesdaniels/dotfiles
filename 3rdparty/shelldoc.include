# symlink shelldoc in place

printf "INFO: installing shelldoc..."

# this assumes ~/bin is where binaries go
rm -f "$HOME/bin/shelldoc"
ln -s "$THIRDPARTY_DIR/shelldoc/shelldoc" "$HOME/bin/shelldoc"

echo " DONE"
